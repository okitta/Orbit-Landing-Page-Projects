import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Toaster } from '@/components/ui/toaster';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Abrish Leather - Premium Ethiopian Leather Craftsmanship',
  description: 'Experience authentic Ethiopian leather craftsmanship. Discover our collection of handcrafted leather bags, wallets, and accessories made with traditional artistry.'
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}